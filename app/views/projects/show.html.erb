<h1><%= @project.name %></h1>
<p><%= @project.description %></p>

<%= link_to('Delete', @project, method: :delete, data: {confirm: 'Confirm delete?'}) %>
<%= link_to('Edit', edit_project_path(@project)) %>
